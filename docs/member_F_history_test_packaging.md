# 成员 F：历史查询 / 测试 / 打包模块

## 负责模块

历史查询页面完善、查询结果导出、手动测试文档维护、最终验收测试和 PyInstaller 打包。

说明：F 不再作为组长分工。项目组长由项目负责人兼任，组长同时参与功能实现，并额外负责协调进度、合并代码、处理冲突和组织答辩。

## 主要文件

| 文件 | 说明 |
|---|---|
| `ui/main_window.py` | 记录查询 Tab 和查询结果表格 |
| `database/db_helper.py` | 历史查询数据库接口 |
| `tests/final_acceptance_test.md` | 最终验收测试 |
| `tests/*.md` | 各模块手动测试文档 |
| `README.md` | 打包与测试说明 |
| `GIT_GUIDE.md` | Git 协作规范 |

## 需要实现的功能

- 完善记录查询 Tab。
- 支持按日期范围查询。
- 支持按样品编号模糊查询。
- 支持按操作员查询。
- 支持双击查看试验详情。
- 支持导出查询结果。
- 维护最终验收测试表。
- 使用 PyInstaller 打包项目。

## 与其他成员的接口

| 成员 | 接口 |
|---|---|
| A | 使用 `DbHelper.query_tests()` 和后续历史查询接口 |
| B | 验证状态机完整流程是否符合验收测试 |
| C | 检查温度曲线和记录数据是否能支撑验收 |
| D | 检查 CSV、Excel、PDF 是否能生成 |
| E | 配合记录查询 Tab 和界面交互优化 |

## 测试方法

按 `tests/final_acceptance_test.md` 从启动程序跑到生成报告、历史查询、校准保存和打包。每次合并新功能后至少执行相关模块的手动测试。

## 最终交付物

历史查询功能可用，测试文档完整，最终验收流程通过，项目能成功打包为 exe。

## TODO 清单

- TODO[F]: 完善按日期范围查询。
- TODO[F]: 增加按操作员查询。
- TODO[F]: 增加历史查询结果导出。
- TODO[F]: 执行最终验收测试并记录结果。
- TODO[F]: 检查 PyInstaller 打包后的路径问题。
